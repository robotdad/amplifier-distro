I'm using the writing-plans skill to create the implementation plan.

Plan saved to `docs/plans/2026-02-27-chat-to-voice.md` (49KB).

---

## What the plan covers

**8 tasks, fully TDD, all with exact file paths and complete code:**

| Task | What it builds | Phase |
|------|---------------|-------|
| 1 | `_extract_text_turns()` — normalizes Amplifier `transcript.jsonl` across chat/CLI/Slack formats | Phase 2 prereq |
| 2 | `_build_chat_context()` — reads `handoff.md` + transcript, formats as Realtime API items | Phase 2 |
| 3 | `_build_context_for_resume()` — dispatcher that branches on `created_by_app` | Phase 1 refactor |
| 4 | Stamp `created_by_app: "voice"` into voice session `metadata.json` | Phase 1 prereq |
| 5 | Wire dispatcher into `resume_session()` route + integration test | Phase 3 |
| 6 | Widen `GET /sessions` to merge voice + non-voice sessions | Phase 4 prereq |
| 7 | Update `SessionPicker` with source app labels | Phase 4 |
| 8 | Type checks + full regression | Cleanup |

**Key findings from codebase exploration that shaped the plan:**

- The new helpers live in a new file `transcript/context.py` — the existing `voice/__init__.py` is already 693 lines
- `metadata.json` doesn't currently write `created_by_app` for voice sessions — Task 4 adds that stamp
- `GET /sessions` currently returns only from `voice-sessions/index.json` — Task 6 merges in the Amplifier projects store
- `SessionPicker` has **no voice-only filter** in the JS — it just shows whatever the API returns, so widening the API (Task 6) is the right lever for Task 7

---

**Two execution options:**

**1. Subagent-Driven (this session)** — I dispatch a fresh subagent per task, review between tasks

**2. Parallel Session (separate)** — Open a new session with the plan doc, batch execution with checkpoints

Which approach?

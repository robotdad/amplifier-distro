Done. Here's what was written and committed as `7b7ba87`:

**`docs/plans/2026-02-27-chat-to-voice-design.md`** — fully restructured from the prior Q&A brainstorm draft into a clean specification with these sections:

| Section | Contents |
|---|---|
| **Overview** | What's being built, why it exists, the AMPLIFIER_HOME_CONTRACT context |
| **Requirements** | 8 functional requirements + 5 non-functional constraints (no new infra, no client breakage, etc.) |
| **Design** | ASCII architecture diagram, context item format, `created_by_app` discriminator rationale |
| **Components** | `resume_session()`, `_build_context_for_resume()`, `_build_chat_context()`, `SessionPicker` — each with interface signatures and behavior |
| **Implementation Approach** | 4 phases in order (refactor → build → wire → frontend), with the transcript format audit called out as a prerequisite |
| **Testing Strategy** | 3 unit tests + 1 integration test + manual verification checklist + regression coverage |
| **Open Questions** | `recent_turns` default, transcript format variance, `SessionPicker` filter criteria, voice model system prompt |

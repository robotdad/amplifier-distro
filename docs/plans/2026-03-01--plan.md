Plan saved to `docs/plans/2026-03-01-chat-to-voice-plan.md`. Here's what was produced:

---

## Overview

The plan covers **9 tasks across 5 phases**, derived directly from the design doc and verified against the actual codebase (live file paths, real test patterns, confirmed imports).

## Tasks at a glance

| Task | What | Phase |
|------|------|-------|
| **1** | Create `context.py` + `_extract_text_turns()` — JSONL normalizer | 1 |
| **2** | `_read_session_created_by_app()` — metadata reader | 2 |
| **3** | `_build_chat_context()` — preamble + recent turns builder | 2 |
| **4** | `_build_context_for_resume()` — voice/non-voice dispatcher | 2 |
| **5** | Stamp `created_by_app: "voice"` in `repository.py` on create | 3 |
| **6** | Wire dispatcher into `resume_session()` route | 3 |
| **7** | `_list_amplifier_sessions()` + widen `list_sessions()` | 4 |
| **8** | `APP_LABELS` + `created_by_app` rendering in `SessionPicker` | 4 |
| **9** | pyright + ruff + full regression gate | 5 |

## Key decisions captured in the plan

- **34 new tests** across 8 test classes, all matching the exact class/method names from the design doc's testing strategy
- **OQ-4 explicitly flagged** with two resolution options — the plan notes that `FoundationBackend.get_session_info()` is in-memory only, so cross-process chat sessions will 404 on resume until a filesystem fallback is added
- **Tasks 5, 7, 8 are parallelizable** once Tasks 1–4 pass — noted in the dependency graph
- **All file paths verified** against the actual directory tree (`distro-server/src/amplifier_distro/server/apps/voice/...`)
- **Test patterns match the existing codebase** — `tmp_path` fixture, `_repo_override`/`_backend_override`, `TestClient(_make_app())`, class-based structure

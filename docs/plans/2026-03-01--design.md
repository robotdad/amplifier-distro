The design document has been written and committed (`1024410`). Here's a summary of what was produced:

---

**`docs/plans/2026-03-01-chat-to-voice-design.md`** — Chat-to-Voice Session Handoff Design

| Section | Contents |
|---|---|
| **Overview** | What's being built and why — voice resumption of chat/CLI/Slack sessions |
| **Requirements** | 8 functional requirements + 5 non-functional constraints (no new infra, additive-only shared code, TDD, client unchanged) |
| **Design** | ASCII architecture diagram, `created_by_app` discriminator rationale, Realtime context item format |
| **Components** | 7 components with signatures: `_extract_text_turns`, `_build_chat_context`, `_build_context_for_resume`, `_read_session_created_by_app`, `_stamp_created_by_app`, `_list_amplifier_sessions`, `SessionPicker` |
| **Data Flow** | Full walk-through for chat resume path and voice-to-voice (regression) path |
| **Error Handling** | 7 scenarios: missing files, malformed metadata, write failures, legacy sessions, long transcripts |
| **Implementation Approach** | 5 phases in dependency order with a parallelism note for Phase 4 |
| **Testing Strategy** | 4 test classes with specific test names, static content tests, and manual verification checklist |
| **Open Questions** | 5 open items: `recent_turns` default, SessionPicker filter, system prompt hint, cross-process session discovery, cross-project grouping |

The existing `delegation-feedback-design.md` was left untouched — it was already complete. This document covers the companion feature (chat-to-voice handoff) that had an implementation plan but was missing its design doc.
